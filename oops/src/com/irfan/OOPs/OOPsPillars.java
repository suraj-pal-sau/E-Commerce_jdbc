package com.irfan.OOPs;

public class OOPsPillars {
	/*
	 	1)Encapsulation: bind the data and method(Hiding data)
	 		> wrapping class -> variables(data) + methods(behavior) into a single unit
	 		>>> Encap. is the process of wrapping code and data together into 
				a single unit declare variables as private, 
				declare the setter and getter to set and get the variable values
	 		> need to hide data directly and provide some method to access or update data
	 	2)Abstraction: Hiding Implementation
	 		> we will hide internal logics and show required features/functions (a remote internal circuit is hidden, how is it working -> hidden )
	 		> expose reqd functions(adv: users don't have to be worry about how it is working/its complexities)
	 	3)Inheritance: Re-using code
	 		> your room mate using your notes(adv : he does not have to xerox -> time save, money save, trust-> paper quality will not be better, machines default) 
	 	4)Polymorphism: multiple forms based on situation
	 		> a person in school->student, in pg -> tenant, bus/train -> passenger, in hospital-> patient, in friend circle-> mentor giving advise
	 		> one person behaves differently in different situation
	 		> same object behaves differently based on different situation
	 */
}
